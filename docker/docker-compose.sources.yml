# docker-compose.sources.yml
# Host sources directory mount — Generated by install.sh

services:
  claude-a:
    volumes:
      - "${SOURCES_DIR}:/sources"

  claude-b:
    volumes:
      - "${SOURCES_DIR}:/sources"
