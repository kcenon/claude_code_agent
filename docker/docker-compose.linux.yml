# docker-compose.linux.yml
# Linux-specific UID/GID mapping — Generated by install.sh

services:
  claude-a:
    user: "${UID}:${GID}"
    environment:
      - HOME=/home/node

  claude-b:
    user: "${UID}:${GID}"
    environment:
      - HOME=/home/node

